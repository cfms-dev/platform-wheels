--- cffi-2.0.0-orig/setup.py	2025-11-13 02:30:56.484275022 +0000
+++ cffi-2.0.0/setup.py	2025-11-13 02:31:11.459382567 +0000
@@ -9,10 +9,19 @@
 
 sources = ['src/c/_cffi_backend.c']
 libraries = ['ffi']
-include_dirs = ['/usr/include/ffi',
-                '/usr/include/libffi']    # may be changed by pkg-config
+
+# For cross-compilation, use environment variables if set
+include_dirs = []
+if os.environ.get('FFI_INCLUDE_DIR'):
+    include_dirs.append(os.environ['FFI_INCLUDE_DIR'])
+    print(f"Using FFI_INCLUDE_DIR: {os.environ['FFI_INCLUDE_DIR']}")
+
 define_macros = [('FFI_BUILDING', '1')]   # for linking with libffi static library
 library_dirs = []
+if os.environ.get('FFI_LIB_DIR'):
+    library_dirs.append(os.environ['FFI_LIB_DIR'])
+    print(f"Using FFI_LIB_DIR: {os.environ['FFI_LIB_DIR']}")
+
 extra_compile_args = []
 extra_link_args = []
 
@@ -150,7 +159,7 @@
         sources.extend(os.path.join(COMPILE_LIBFFI, filename)
                     for filename in _filenames)
 else:
-    use_pkg_config()
+    pass  # Don't use pkg-config for cross-compilation
     ask_supports_thread()
     ask_supports_sync_synchronize()
 
--- cffi-2.0.0-orig/src/c/malloc_closure.h	2025-11-13 02:30:56.486275036 +0000
+++ cffi-2.0.0/src/c/malloc_closure.h	2025-11-13 02:31:23.106464086 +0000
@@ -42,6 +42,9 @@
         return 0;
     ret = 0;
 
+
+    /* Chaquopy: getline requires API level 18, but PaX isn't used by Android anyway. */
+    #if __ANDROID_API__ >= 18
     while (getline (&buf, &len, f) != -1)
         if (!strncmp (buf, "PaX:", 4))
             {
@@ -51,6 +54,8 @@
                 break;
             }
     free (buf);
+    #endif
+
     fclose (f);
     return ret;
 }
